= form_with(model: estimation_of_task, local: true) do |form|
  - if estimation_of_task.errors.any?
    #error_explanation
      %h2
        = pluralize(estimation_of_task.errors.count, "error")
        prohibited this estimation_of_task from being saved:
      %ul
        - estimation_of_task.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :title
    = form.text_field :title
  .field
    = form.label :detail
    = form.text_field :detail
  .field
    = form.label :task_type
    = form.number_field :task_type
  .actions
    = form.submit
